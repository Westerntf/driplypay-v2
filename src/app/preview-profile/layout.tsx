/**
 * 🚀 Developer Reference: /BLUEPRINT.md → Public Profile Features
 * Layout: Preview profile layout for testing profile appearance
 * Features: Clean layout without navigation for profile preview
 */

export default function PreviewProfileLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}